// import CryptoJS from 'crypto-js';

// const Encrypt = (word, key) => {
//   const encryptMessage = CryptoJS.AES.encrypt(word, key).toString();
//   return encryptMessage;
// };
// const Decrypt = async (word, key) => {
//   const decryptMessage = await CryptoJS.AES.decrypt(word, key).toString(
//     CryptoJS.enc.Utf8,
//   );
//   return decryptMessage;
// };

// export {Encrypt, Decrypt};
